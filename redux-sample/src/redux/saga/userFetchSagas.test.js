// import { call, put } from "redux-saga/effects";
// import FetchRootSaga from "./userFetchSagas";

// import * as actions from "../actions/userActions";
// import * as types from "../actions/actionTypes";

describe("the fetching user data", () => {
  it("It fetch data and set into state", () => {
    // const users = [
    //   { id: 1, name: "a" },
    //   { id: 2, name: "b" },
    // ];
    // const json = () => {};
    // const response = { json };
    // const gen = FetchRootSaga();

    // expect(gen.next("loading").value).toEqual(
    //   put({ type: types.BEGIN_API_CALL, value: "loading" })
    // );
  });
});
